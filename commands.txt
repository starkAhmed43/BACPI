conda run -n mldb python emulator_bench/cache_embeddings.py --value_type ki

conda run -n mldb python emulator_bench/cache_embeddings.py --value_type ki kd ec50

conda run -n mldb python emulator_bench/train_single_target_tvt.py --value_type ki --split_group random_splits

conda run -n mldb python emulator_bench/train_single_target_tvt.py --value_type kd --split_group enzyme_sequence_splits --threshold threshold_0.01

conda run -n mldb python emulator_bench/run_split_benchmarks.py --value_type ec50 --split_groups random_splits uniprot_time_splits enzyme_sequence_splits enzyme_structure_splits substrate_splits conformer_cosine_splits --seeds 666 667

conda run -n mldb python emulator_bench/tune_optuna.py --value_type ic50 --split_groups random_splits enzyme_sequence_splits substrate_splits --seeds 666 --n_trials 40 --study_name bacpi_ic50_optuna

conda run -n mldb python emulator_bench/launch_parallel_optuna.py --gpus 0 1 2 3 --trials_per_gpu 1 --base_dir ~/github/EMULaToR/data/processed/baselines/BACPI/ki --embeddings_dir ~/github/EMULaToR/data/processed/baselines/BACPI/ki/embeddings --split_groups random_splits enzyme_sequence_splits substrate_splits --seeds 666 --n_trials 32 --study_name bacpi_ki_optuna --storage sqlite:////home/adhil/github/EMULaToR/data/processed/baselines/BACPI/ki/optuna_studies/bacpi_ki_optuna.db

conda run -n mldb python emulator_bench/launch_parallel_retrain_from_optuna.py \
    --gpus 0 1 2 3 \
    --trials_per_gpu 1 \
    --value_type ki kd ec50 \
    --split_groups random_splits uniprot_time_splits enzyme_sequence_splits enzyme_structure_splits substrate_splits \
    --seeds 666 777 888 \
    --study_name bacpi_ki_optuna \
    --storage sqlite:////home/adhil/github/EMULaToR/data/processed/baselines/BACPI/ki/optuna_studies/bacpi_ki_optuna.db

conda run -n mldb python emulator_bench/launch_parallel_retrain_from_optuna.py \
    --gpus 0 1 \
    --trials_per_gpu 6 \
    --value_type ki \
    --split_groups random_splits uniprot_time_splits enzyme_sequence_splits enzyme_structure_splits substrate_splits \
    --seeds 666 777 888 \
    --hparams_json /home/adhil/github/BACPI/emulator_bench/default_hparams_original.json \
    --skip_smiles_validity_check \
    --num_workers 4

for value_type in ki kd ec50 ic50; do conda run -n mldb python emulator_bench/cache_embeddings.py --value_type "${value_type}"; done

for value_type in ki kd ec50 ic50; do conda run -n mldb python emulator_bench/run_split_benchmarks.py --value_type "${value_type}" --split_groups random_splits uniprot_time_splits enzyme_sequence_splits enzyme_structure_splits substrate_splits conformer_cosine_splits --seeds 666; done

